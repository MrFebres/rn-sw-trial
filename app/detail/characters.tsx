import { View, Text } from 'react-native';
import React from 'react';

const CharactersTab = () => {
   return (
      <View>
         <Text>CharactersTab</Text>
      </View>
   );
};

export default CharactersTab;
